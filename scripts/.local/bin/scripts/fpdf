#!/bin/sh -e

pdf=$(find ~/Documents/books ~/Documents/sci \
           -mindepth 1 -maxdepth 1 -type f -iname '*.pdf' 2>/dev/null \
      | fzf --delimiter '/' --with-nth='-1')

test -n "${pdf}" && open "${pdf}"
